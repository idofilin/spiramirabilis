\newcommand\logslantsolZ{\frac{\gamma s_{0} \sqrt{1 - c^{2}} \left(- c \cos{\left(c \theta + \lambda_{0} \right)} + \gamma \sin{\left(c \theta + \lambda_{0} \right)}\right) e^{\gamma \theta}}{c^{2} + \gamma^{2}}}%
\newcommand\logslantsolX{- c^{4} \cos{\lambda } \cos{\theta } + c^{3} \gamma \sin{\lambda } \cos{\theta } - 2 c^{3} \sin{\lambda } \sin{\theta } - c^{2} \gamma^{2} \cos{\lambda } \cos{\theta } - 3 c^{2} \gamma \sin{\theta } \cos{\lambda } + c \gamma^{3} \sin{\lambda } \cos{\theta } + 3 c \gamma \sin{\lambda } \cos{\theta } + 2 c \sin{\lambda } \sin{\theta } - \gamma^{3} \sin{\theta } \cos{\lambda } + \gamma^{2} \cos{\lambda } \cos{\theta } - \gamma \sin{\theta } \cos{\lambda } + \cos{\lambda } \cos{\theta }}%
\newcommand\logslantsolY{- c^{4} \sin{\theta } \cos{\lambda } + c^{3} \gamma \sin{\lambda } \sin{\theta } + 2 c^{3} \sin{\lambda } \cos{\theta } - c^{2} \gamma^{2} \sin{\theta } \cos{\lambda } + 3 c^{2} \gamma \cos{\lambda } \cos{\theta } + c \gamma^{3} \sin{\lambda } \sin{\theta } + 3 c \gamma \sin{\lambda } \sin{\theta } - 2 c \sin{\lambda } \cos{\theta } + \gamma^{3} \cos{\lambda } \cos{\theta } + \gamma^{2} \sin{\theta } \cos{\lambda } + \gamma \cos{\lambda } \cos{\theta } + \sin{\theta } \cos{\lambda }}%
\newcommand\logslantsolXYdenom{c^{4} + 2 c^{2} \gamma^{2} - 2 c^{2} + \gamma^{4} + 2 \gamma^{2} + 1}%
\endinput